"use strict"

const chai = require('chai'),
      sinon = require('sinon'),
      lolex = require('lolex'),
      sinonChai = require('sinon-chai');

const should = chai.should();
chai.use(sinonChai);

// should require a valid db + clientinfo driver
// should timeout clients who don't discover properly?


// drivers - clientinfo
// extra config? like mac vs hostname
// should return mac and hostname
// should not signal ready if none

// drivers - filepath
// should require extra config, dbpath
// should fail on no file AND no global
// should return global only
// should return file only
// should merge file + global

describe('Main', function() {

});